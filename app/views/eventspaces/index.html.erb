<%#
<ul class="cards"><% @eventspaces.each do |e| %>
  <%# <li class="card-category" style= "background-image: url(https://blog.printsome.com/wp-content/uploads/abcs-restaurants-hero-image.jpg)"><%= link_to "#{e.name}", eventspace_path(e) %></li>
  <%# <% end </ul> %>

<ul class="cards"><% @eventspaces.each do |e| %>

<div class="card-trip">
  <%= cl_image_tag e.photos.first.key %>

  <div class="card-trip-infos">
    <div>
      <h2><%= link_to "#{e.name}", eventspace_path(e) %></li></h2>
      <p><%= "#{e.description}"%></p>
    </div>
    <h2 class="card-trip-pricing"><%= "$#{e.price}"%></h2>

  </div>
</div>
  <% end %> </ul>
